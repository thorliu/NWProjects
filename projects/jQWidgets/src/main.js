define(function ()
{
	var moduleName = "main";
	var moduleVersion = "1.0.0";

	var run = function()
	{
		alert("?");
	};

	return {
		moduleName: moduleName,
		version: moduleVersion,
		run: run
	};
});